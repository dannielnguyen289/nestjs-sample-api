import { UnitOwnerInterceptor } from './unit-owner.interceptor'

describe('UnitOwnerInterceptor', () => {
  it('should be defined', () => {
    expect(new UnitOwnerInterceptor()).toBeDefined()
  })
})
